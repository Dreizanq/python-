s = input().strip()

max_streak = 0
current_streak = 0

for char in s:
    if char == 'Р':
        current_streak += 1
        if current_streak > max_streak:
            max_streak = current_streak
    else:
        current_streak = 0

print(max_streak)
